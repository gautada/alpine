#!/bin/ash

# container-backup.fnc
#
# Provides a testing container_backup function.  This is
# just for testing or example.

container_backup() {
 pwd
 date > alpine-backup.txt
 FOLDER=$(date +%s)
 mkdir $FOLDER
 date > $FOLDER/another-backup.txt
}
