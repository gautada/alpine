#!/bin/sh
#
# backup: This script defines the default backup mechanism function and should be overloaded by downstream controls

container_backup() {
 echo "[ INFO] Current directory($(PWD))"
 echo "[ WARN] Container backup function not customized for container"
 /bin/cp -r /mnt/volumes/container/* /tmp/backup
 return $?
}
